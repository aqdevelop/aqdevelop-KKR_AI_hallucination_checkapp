# AI 할루시네이션 검증 앱 — 5루프 기획

> 한 줄 요약: AI가 쓴 글을 붙여넣으면, 사실 주장(claim)을 인터넷에서 교차검증해
> "할루시네이션 의심 구간"을 표시하고, 진짜 정보로 대체 제안까지 해주는 Flutter 앱.

---

## 루프 1 — 컨셉 / MVP 정의 (Why & What)

### 1.1 문제 정의
- ChatGPT·Claude·Gemini 등이 만든 글에는 **그럴듯하지만 틀린 사실**이 자주 섞여
  있다. 일반 사용자는 어디가 틀렸는지 알 길이 없다.
- 기존 팩트체크 도구는 (1) 영어 위주, (2) 뉴스 한 문장 단위, (3) AI 글의
  특수성(긴 문단, 주장 밀도 높음)에 안 맞음.

### 1.2 타깃 사용자
- 1순위: AI로 보고서/블로그 초안을 쓰는 직장인·학생.
- 2순위: AI 답변을 그대로 인용하기 전에 검증하고 싶은 사람.

### 1.3 가치 제안 (한 줄)
- "복붙 한 번에, 어느 문장이 거짓말인지 색칠해주는 앱."

### 1.4 MVP 핵심 기능 (이 4개만 된다면 출시 가능)
1. **텍스트 입력**: AI가 쓴 글을 붙여넣기.
2. **주장 추출**: 입력 글에서 사실 주장(verifiable claim) 단위로 쪼갬.
3. **검증**: 각 주장을 인터넷 검색 → 일치/불일치/근거 부족 분류.
4. **결과 시각화**: 원문 위에 색깔로 하이라이트(초록=확인, 노랑=불확실, 빨강=의심)
   + 의심 구간엔 대체 문장 제안.

### 1.5 비-목표 (지금은 안 함)
- 이미지/영상 검증, 실시간 채팅 검증, 사용자가 작성한 일반 글 검증,
  법적 효력 있는 팩트체크.

### 1.6 루프 1의 한계 (다음 루프에서 풀 것)
- "주장 추출"이 어떻게 가능한가? — 알고리즘 미정.
- 검색 결과가 신뢰할 만한지 어떻게 판단? — 출처 신뢰도 모델 없음.
- "대체 문장 제안"의 출력 형태 미정.

---

## 루프 2 — 기술 스택 / 아키텍처 (How, 시스템)

### 2.1 전체 흐름도
```
[Flutter 앱]
   │  (1) 사용자가 텍스트 붙여넣기
   ▼
[백엔드 API] (Cloud Functions or FastAPI)
   │  (2) LLM #1 — 주장 추출 (claim extraction)
   │  (3) 각 주장별 검색 쿼리 생성
   ▼
[검색 레이어] (Google Programmable Search / Bing / Brave Search API)
   │  (4) 상위 N개 결과(스니펫+URL) 수집
   ▼
[검증 레이어]
   │  (5) LLM #2 — 주장 vs 스니펫 NLI(함의/모순/중립)
   │  (6) 출처 신뢰도 점수와 가중 평균
   ▼
[교정 레이어]
   │  (7) 의심 주장에 대해 LLM #3 — 검색 스니펫 기반 대체 문장 생성(인용 포함)
   ▼
[Flutter 앱]
       (8) 색상 하이라이트 + 사이드 패널에 근거/대체안
```

### 2.2 기술 선택
| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트 | Flutter (Dart) | 모바일+웹 동시, 사용자가 익숙해질 예정 |
| 상태관리 | Riverpod | Provider보다 안전, 비동기 처리 편함 |
| 네트워크 | `dio` + `retrofit` | 인터셉터/에러 처리 깔끔 |
| 백엔드 | Python + FastAPI | LLM 라이브러리 풍부, 빠른 프로토타이핑 |
| 호스팅 | Cloud Run (또는 Vercel + Edge Functions) | 무중단 + 사용량 과금 |
| LLM | Claude Haiku 4.5 (저비용 추출) + Sonnet 4.6 (검증·교정) | 비용/품질 분리 |
| 검색 | Brave Search API or Google CSE | Brave는 LLM 친화적 스니펫 |
| DB | Firestore (캐시·사용자 히스토리) | 서버리스, Flutter 연동 쉬움 |
| 인증 | Firebase Auth (이메일+구글) | 무료 한도 충분 |

### 2.3 모듈 구조 (Flutter 측)
```
lib/
├── main.dart
├── core/         # theme, router, di
├── data/
│   ├── api/      # FactCheckApi (dio)
│   └── models/   # Claim, Verdict, Source
├── domain/
│   └── usecases/ # CheckTextUseCase
├── features/
│   ├── input/    # 입력 화면
│   ├── result/   # 결과 화면 (하이라이트)
│   └── history/  # 검사 기록
└── widgets/      # 공통 위젯 (HighlightedText 등)
```

### 2.4 API 계약 (초안)
```
POST /v1/factcheck
Request:  { "text": "...", "language": "ko" }
Response: {
  "claims": [
    {
      "id": "c1",
      "span": { "start": 102, "end": 158 },
      "text": "세종대왕은 1418년에 즉위했다",
      "verdict": "supported" | "refuted" | "unverifiable",
      "confidence": 0.92,
      "sources": [ { "url": "...", "title": "...", "snippet": "...", "trust": 0.8 } ],
      "suggestion": "세종대왕은 1418년 8월에 즉위했다(조선왕조실록)."
    }
  ],
  "summary": { "total": 12, "supported": 8, "refuted": 2, "unverifiable": 2 }
}
```

### 2.5 루프 2의 한계
- LLM 호출이 글 한 편당 3종류 × N개 주장 → **비용 폭발 위험**.
- 한국어 검색 결과 품질이 영어보다 떨어짐 — 출처 신뢰도 산정 어려움.
- 결과 응답시간(아마 10~30초) — UX 어떻게 견디게 만들 것인가?

---

## 루프 3 — 핵심 알고리즘 디테일 (할루시네이션 어떻게 잡나)

### 3.1 주장 추출 (Claim Extraction)
- 입력 텍스트를 문장 단위 분리 → 각 문장에 대해:
  - **검증 가능 여부** 분류 (의견/주관 vs 사실 주장).
  - 사실 주장이면 **원자적 주장(atomic claim)** 으로 분해.
    예: "이순신은 1545년 서울에서 태어났고 명량해전을 이끌었다"
       → ["이순신은 1545년에 태어났다", "이순신은 서울에서 태어났다",
          "이순신이 명량해전을 이끌었다"]
- 프롬프트 출력은 JSON 강제(스키마 검증). 실패 시 1회 재시도.

### 3.2 검색 쿼리 생성
- 주장에서 **개체(entity)** + **속성(attribute)** + **값(value)** 추출.
  - "이순신 출생연도", "명량해전 지휘관" 같은 형태.
- 한국 인물/사건은 한국어 쿼리, 영어권이면 영어 쿼리 자동 결정.

### 3.3 증거 수집
- 검색 API 상위 5~8개 결과의 (제목, URL, 스니펫) 수집.
- 도메인별 **신뢰도 가중치 테이블** 적용 (정적 + 학습형):
  - 정부·공공기관(.go.kr, .gov), 위키피디아, 주류 언론사 → 0.8~0.9
  - 일반 블로그, 커뮤니티 → 0.2~0.4
  - AI 생성 콘텐츠 의심 사이트(블로그팜) → 0.0
- 중복 도메인은 1회만 카운트(에코 챔버 방지).

### 3.4 검증 (NLI 기반 판정)
- 각 (주장, 스니펫) 쌍을 LLM에 입력 → **entail / contradict / neutral** 판정.
- 가중 합계로 verdict 결정:
  - supported: Σ(entail × trust) ≥ 임계값 & contradict 거의 없음
  - refuted: Σ(contradict × trust) 가 명확히 우위
  - unverifiable: 둘 다 부족
- 단일 출처만으로 supported 판정 금지(최소 2 독립 도메인).

### 3.5 대체 문장 생성
- refuted 판정 주장에 한해서만 실행(비용 절감).
- 입력: 원문 주장 + 가장 신뢰도 높은 contradict 스니펫 2~3개.
- 출력 규칙:
  - 원문 톤·길이 최대한 유지.
  - 끝에 `[1]` 형태 인용 표기, 출처는 사이드 패널에서 보이게.
  - 모르겠으면 "확인 불가"로 명시(또 다른 할루시네이션 만들지 않기).

### 3.6 안티-할루시네이션 가드레일
- LLM이 **검색 결과에 없는 사실**을 만들지 못하도록 시스템 프롬프트에
  "주어진 스니펫 외 정보는 추측 금지" 명시.
- 자기검증: 생성된 대체 문장을 다시 NLI 통과시켜 entail 확인.

### 3.7 캐싱 전략
- (정규화된 주장 텍스트) → verdict + sources 캐시 (Firestore, TTL 30일).
- 동일 글 재검사 비용 0에 수렴.

### 3.8 루프 3의 한계
- 신뢰도 가중치 테이블은 결국 사람이 만들어야 함 — 초기에 어떻게 채울지.
- "의견 vs 사실" 분류 자체가 회색지대 많음.
- 한국어 NLI 정확도 검증 안 됨 — 평가셋 필요.

---

## 루프 4 — UX / 화면 설계

### 4.1 핵심 화면
1. **홈/입력 화면**
   - 큰 텍스트 영역 (붙여넣기 유도 placeholder: "AI가 써준 글을 여기에…")
   - "검사 시작" 버튼.
   - 하단에 최근 기록 3개.

2. **진행 중 화면**
   - 단계별 진행 표시: 문장 분석 → 검색 → 검증 → 교정 (4단계 progress).
   - 평균 소요시간/예상 잔여시간 (캐시 히트율 반영).
   - **중간에 결과를 점진적으로 노출** (스트리밍): 분석 끝난 문장부터 색칠.

3. **결과 화면**
   - 상단 요약 배지: `🟢 8 / 🟡 2 / 🔴 2` 형태 + 신뢰 점수 100점 환산.
   - 본문에 색깔 하이라이트(접근성 위해 색+밑줄 패턴 동시).
   - 빨강/노랑 구간 **탭 시 사이드 시트 확장**:
     - 왜 의심되는가 (모순되는 출처 미리보기)
     - 추천 대체 문장 (한 번 탭으로 본문에 적용)
     - 출처 링크들
   - 우상단: "교정본 전체 적용" / "공유" / "저장".

4. **기록 화면**
   - 검사한 글 리스트, 신뢰 점수, 다시 보기.

5. **설정**
   - 언어, 검색 엔진 선택, 도메인 신뢰도 커스터마이즈(고급).

### 4.2 UX 원칙
- **느리다는 걸 숨기지 말고 보여주기**: 진행률 + 부분 결과 = 체감 속도 향상.
- **Undo 가능한 적용**: 대체 문장은 언제든 원문 복귀 가능해야 신뢰가 생김.
- **출처 1초 이내 도달**: 모든 빨강/노랑 구간은 두 번 탭 안에 원본 URL.
- **읽기 모드/검사 모드 토글**: 색깔이 부담스러울 때 일반 텍스트로 보기.

### 4.3 빈 상태 / 에러 상태
- 입력 너무 짧음(≤30자): "한 문단 이상 넣어주세요" 안내.
- 검색 실패: "출처를 못 찾았어요. 같은 글 다시 시도하기" + 부분 결과 노출.
- 오프라인: 큐에 저장 → 온라인 시 자동 재시도.

### 4.4 접근성
- 색맹 모드: 색 외에 패턴(점선/물결밑줄/체크마크).
- 폰트 크기 조절, 스크린리더 라벨링, 키보드 내비게이션(웹 빌드).

### 4.5 핵심 인터랙션 한 컷
> 빨간 구간 탭 → 시트 슬라이드업 → 좌측 "왜?" / 우측 "이렇게 써보세요" →
> 우측을 탭하면 본문이 부드럽게 교체 + 토스트로 "되돌리기" 5초.

### 4.6 루프 4의 한계
- 사이드 시트가 모바일에선 좁음 — 가로 모드/태블릿 별도 레이아웃 필요.
- 실시간 스트리밍 결과를 Flutter에서 어떻게 받을지(SSE? WebSocket?) 미정.

---

## 루프 5 — 리스크 / 비용 / 일정 / 로드맵

### 5.1 리스크와 대응
| 리스크 | 영향 | 대응 |
|---|---|---|
| LLM 비용 폭주 | 사업성 위협 | 캐시 + Haiku로 추출, Sonnet은 의심구간만 / 사용자별 일일 쿼터 |
| 잘못된 판정(오탐) | 신뢰 붕괴 | "supported"라도 "확인된 출처" 라벨로 표현, 100% 단정 금지 |
| 검색 API 정책 변경 | 서비스 중단 | 어댑터 패턴으로 다중 백엔드(Brave/Google/Bing) 스왑 |
| 한국어 NLI 품질 | 핵심 기능 저하 | 자체 평가셋 200개 운영, 회귀 모니터링 |
| 저작권/크롤링 | 법적 이슈 | 스니펫만 표시, 본문 저장 X, 사용자 입력은 암호화 |
| 사용자가 결과를 맹신 | 사회적 리스크 | "참고용" 문구 상시 노출, 신뢰점수에 신뢰구간 표기 |

### 5.2 비용 추정 (글 1개 = 평균 10주장 기준)
- 추출(Haiku): ~1k 토큰 입력 → 약 $0.001
- 검증(Sonnet, 10주장 × 3스니펫): ~6k 입력 + 1k 출력 → 약 $0.03
- 교정(Sonnet, 의심 2개): ~1k → 약 $0.005
- 검색 API: 10쿼리 × $0.002 = $0.02
- **글 1개당 ≈ $0.05** (캐시 히트 시 50% 절감 목표)
- 무료 사용자: 일 3회, 유료: 무제한 ₩4,900/월 가설.

### 5.3 일정 (혼자 + AI 보조 기준)
- W1: Flutter 입력 화면 + 더미 결과 모킹 + Riverpod 골격
- W2: FastAPI + Claude Haiku 주장 추출 + 단위 테스트
- W3: 검색 API 통합 + 신뢰도 테이블 v0
- W4: NLI 검증 + verdict 합산 로직
- W5: 결과 화면 하이라이트 + 사이드 시트
- W6: 대체 문장 생성 + 자기검증
- W7: 캐시·인증·사용량 제한·기록 화면
- W8: 평가셋 200개로 정확도 측정 + 클로즈드 베타 10명
- W9~10: 피드백 반영, Play Store 내부 테스트, 약관/개인정보처리방침
- W11: 정식 출시(앱+웹 동시)

### 5.4 성공 지표 (KPI)
- D1: "이게 내가 쓰던 AI 글에 진짜 도움된다"는 정성 피드백 10/10 중 7+
- 정량:
  - 검사 완료율 ≥ 80% (취소 안 하고 끝까지)
  - 대체 문장 채택률 ≥ 30%
  - 4주 리텐션 ≥ 20%
  - 평가셋 정확도: refuted F1 ≥ 0.7

### 5.5 출시 후 로드맵
- v1.1: 브라우저 확장(드래그한 글 즉시 검사)
- v1.2: PDF/문서 업로드 검사
- v1.3: "이 모델이 자주 틀리는 분야" 통계 (사용자가 어떤 LLM 글을 넣었는지 메타)
- v2.0: 팀 공유, 학교/언론사용 워크스페이스
- v2.1: 자체 한국어 NLI 모델로 비용 70% 절감

### 5.6 의사결정 필요 항목 (사용자에게 물을 것)
1. 백엔드 언어 — Python(FastAPI) vs Dart(Cloud Functions for Flutter)? 풀스택
   Dart로 갈 수 있으면 학습 부담 작음.
2. 검색 API — Brave(저렴/LLM친화) vs Google(친숙/고가).
3. 출시 형태 — 모바일 우선 vs 웹 우선? (붙여넣기 UX는 웹이 더 빠름)
4. 수익 모델 — 무료+광고? 구독? B2B 라이선스?
5. 1인 개발 vs 팀 모집? — 일정의 절반은 여기서 결정됨.

---

## 마무리 — 루프 5의 자기비판

- 알고리즘은 그려졌지만 **평가셋 없이는 정확도 모름** — 다음 단계는
  "한국어 할루시네이션 사례 200개 수집"이 1번 작업이어야 함.
- 비용 모델이 LLM API 가격 변동에 취약 — 자체 모델로 가는 v2 로드맵이
  단순 "있으면 좋은 것"이 아니라 **사업 지속성의 전제**.
- UX는 "느림을 숨기지 말자"가 핵심 가설 — 베타에서 가장 먼저 검증할 것.
- 가장 큰 미해결 질문: **사용자가 정말 검증된 글을 원하는가, 빠른 글을
  원하는가?** — 출시 전에 인터뷰 5명으로 가설 검증 필요.
